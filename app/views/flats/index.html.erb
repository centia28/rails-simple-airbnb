<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All our flats (<%= @flats.count %>)</h1>
    </div>
  </div>

  <div class="row">
    <% @flats.each do |flat| %>
      <%= render 'component_flat_card', flat: flat %>
    <% end %>
  </div>
</div>
